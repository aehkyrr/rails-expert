# CodeQL Configuration
# https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/customizing-your-advanced-setup-for-code-scanning
#
# This repository only analyzes GitHub Actions workflows.
# Ruby and JavaScript files in skills/*/examples/ are educational documentation
# snippets, not runnable applications - they are excluded via the workflow matrix.

name: "Rails Expert Plugin CodeQL Config"

# No paths-ignore needed - we only analyze actions language which targets .github/workflows/
